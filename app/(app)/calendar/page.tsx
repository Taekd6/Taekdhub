import { CalendarScreen } from "@/components/calendar/calendar-screen";

export const metadata = { title: "Calendrier — TaekdHub" };

export default function CalendarPage() {
  return <CalendarScreen />;
}
