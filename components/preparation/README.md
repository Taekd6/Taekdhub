# Preparation command

The preparation command is currently integrated in the Dashboard and has a dedicated `/preparation` route. It allocates a requested time budget across active subjects with pending work, then fills remaining time using the existing recommendation engine.
